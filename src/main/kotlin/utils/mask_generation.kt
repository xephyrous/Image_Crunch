package utils

typealias Mask = Array<Array<Int>>;

/*
fun squareMaskGenerator(rows: Int, columns: Int, nodeList: ArrayList<PositionNode>) : Mask {
    var mask: Mask;

    for(y in 0..rows + 2) {
        for(x in 0..columns + 2) {
            
        }
    }

    return mask;
}*/